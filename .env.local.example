# Paid audit booking page. Default: https://cal.com/adrian-barkus-bbcvmp/systems-audit-session
NEXT_PUBLIC_BOOK_AUDIT_URL=

# Required in production (Vercel): GitHub personal access token for the footer contribution heatmap.
# Without it, the GraphQL API returns 403. Create at: https://github.com/settings/tokens
# Classic token with read:user scope (or no scopes for public data).
# GITHUB_TOKEN=
