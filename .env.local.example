# Paid audit booking page. Default: https://cal.com/adrian-barkus-bbcvmp/systems-audit-session
NEXT_PUBLIC_BOOK_AUDIT_URL=

# Required for footer contribution heatmap. Create at: https://github.com/settings/tokens (Classic).
# Without it, the API returns 403. For the count to include ALL repos (including private),
# the token must be the profile user's (TheSpaceFox) and have the "repo" scope.
# GITHUB_TOKEN=

# Required for /ask AI chat. Create at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=
